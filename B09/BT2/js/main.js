const ELEMENT = document.getElementsByClassName('text');
console.log(ELEMENT.toString());
// for (let i = 0; i < ELEMENT.length; i++) {
//     console.log(ELEMENT[i]);
//     ELEMENT.replaceAll('frontend', Element.toUpperCase())
// }